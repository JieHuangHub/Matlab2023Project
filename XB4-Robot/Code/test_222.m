% 测试脚本 - 验证正确答案
clc; clear;

% MDH参数
MDH = [
    0,       0,        0.342,   0;
    0.040,  -pi/2,     0,      -pi/2;
    0.275,   0,        0,       0;
    0.025,  -pi/2,     0.280,   0;
    0,       pi/2,     0,       0;
    0,      -pi/2,     0.073,   0
];

% 正确的关节角度
theta_correct = [0; -0.1201; 0.5164; 0; 0; 0];

% 计算正运动学
T_fk = forwardKinematics_MDH(theta_correct, MDH);

disp('正运动学结果 (使用正确角度):');
disp('位置: ');
disp(T_fk(1:3, 4)');
disp('旋转矩阵:');
disp(T_fk(1:3, 1:3));

% 期望位置
disp('期望位置:  [0.393, 0, 0.64]');
disp('期望姿态: R = [0,0,1; 0,-1,0; 1,0,0]');

% 比较
disp('位置误差:');
disp(T_fk(1:3, 4)' - [0.393, 0, 0.64]);