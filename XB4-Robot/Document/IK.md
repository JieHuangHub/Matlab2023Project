# 6自由度机械臂运动学完整公式链

## 一、符号定义

### 1.1 DH参数（常量）

| 参数 | 值 | 说明 |
|------|------|------|
| $d_1$ | 0.342 | 关节1偏移 |
| $a_1$ | 0.040 | 连杆1长度 |
| $a_2$ | 0.275 | 连杆2长度 |
| $a_3$ | 0.025 | 连杆3长度 |
| $d_4$ | 0.280 | 关节4偏移 |
| $d_t$ | 0.073 | 工具长度 |

### 1.2 三角函数简写

- $c_i = \cos(\theta_i)$，$s_i = \sin(\theta_i)$
- $c_{ij} = \cos(\theta_i + \theta_j)$，$s_{ij} = \sin(\theta_i + \theta_j)$

### 1.3 末端位姿矩阵 $T_{60}$

已知的目标位姿矩阵：

$$
T_{60} = \begin{bmatrix} 
n_x & o_x & a_x & p_x \\ 
n_y & o_y & a_y & p_y \\ 
n_z & o_z & a_z & p_z \\ 
0 & 0 & 0 & 1 
\end{bmatrix}
$$

其中：
- $(n_x, n_y, n_z)^T$：法向量（Normal）
- $(o_x, o_y, o_z)^T$：方向向量（Orientation）
- $(a_x, a_y, a_z)^T$：接近向量（Approach）
- $(p_x, p_y, p_z)^T$：位置向量（Position）

---

## 二、正运动学

### 2.1 各关节变换矩阵

#### 矩阵 $T_{10}$

$$
T_{10} = \begin{bmatrix} 
c_1 & -s_1 & 0 & 0 \\ 
s_1 & c_1 & 0 & 0 \\ 
0 & 0 & 1 & d_1 \\ 
0 & 0 & 0 & 1 
\end{bmatrix}
$$

#### 矩阵 $T_{31}$

$$
T_{31} = T_{21} \cdot T_{32} = \begin{bmatrix} 
c_{23} & -s_{23} & 0 & a_2c_2 + a_1 \\ 
0 & 0 & 1 & 0 \\ 
-s_{23} & -c_{23} & 0 & -a_2s_2 \\ 
0 & 0 & 0 & 1 
\end{bmatrix}
$$

#### 矩阵 $T_{64}$

$$
T_{64} = T_{54} \cdot T_{65} = \begin{bmatrix} 
c_5c_6 & -c_5s_6 & -s_5 & -d_ts_5 \\ 
s_6 & c_6 & 0 & 0 \\ 
s_5c_6 & -s_5s_6 & c_5 & d_tc_5 \\ 
0 & 0 & 0 & 1 
\end{bmatrix}
$$

#### 矩阵 $T_{63}$

$$
T_{63} = T_{43} \cdot T_{64} = \begin{bmatrix} 
n_{x63} & o_{x63} & a_{x63} & P_{x63} \\ 
n_{y63} & o_{y63} & a_{y63} & P_{y63} \\ 
n_{z63} & o_{z63} & a_{z63} & P_{z63} \\ 
0 & 0 & 0 & 1 
\end{bmatrix}
$$

**$T_{63}$ 元素展开：**

| 列 | x分量 | y分量 | z分量 |
|---|-------|-------|-------|
| $\vec{n}_{63}$ | $c_4c_5c_6 - s_4s_6$ | $s_5c_6$ | $-s_4c_5c_6 - c_4s_6$ |
| $\vec{o}_{63}$ | $-c_4c_5s_6 - s_4c_6$ | $-s_5s_6$ | $s_4c_5s_6 - c_4c_6$ |
| $\vec{a}_{63}$ | $-c_4s_5$ | $c_5$ | $s_4s_5$ |
| $\vec{P}_{63}$ | $a_3 - d_tc_4s_5$ | $d_4 + d_tc_5$ | $d_ts_4s_5$ |

### 2.2 最终正运动学 $T_{60} = T_{10} \cdot T_{61}$

$$
T_{60} = \begin{bmatrix} 
r_{11} & r_{12} & r_{13} & p_x \\ 
r_{21} & r_{22} & r_{23} & p_y \\ 
r_{31} & r_{32} & r_{33} & p_z \\ 
0 & 0 & 0 & 1 
\end{bmatrix}
$$

#### 位置向量

$$
p_x = c_1[c_{23}(a_3 - d_tc_4s_5) - s_{23}(d_4 + d_tc_5) + a_2c_2 + a_1] - s_1[d_ts_4s_5]
$$

$$
p_y = s_1[c_{23}(a_3 - d_tc_4s_5) - s_{23}(d_4 + d_tc_5) + a_2c_2 + a_1] + c_1[d_ts_4s_5]
$$

$$
p_z = -s_{23}(a_3 - d_tc_4s_5) - c_{23}(d_4 + d_tc_5) - a_2s_2 + d_1
$$

#### 旋转矩阵

**第一列（法向量 $\vec{n}$）：**

$$
r_{11} = c_1[c_{23}(c_4c_5c_6 - s_4s_6) - s_{23}s_5c_6] - s_1[-s_4c_5c_6 - c_4s_6]
$$

$$
r_{21} = s_1[c_{23}(c_4c_5c_6 - s_4s_6) - s_{23}s_5c_6] + c_1[-s_4c_5c_6 - c_4s_6]
$$

$$
r_{31} = -s_{23}(c_4c_5c_6 - s_4s_6) - c_{23}s_5c_6
$$

**第二列（方向向量 $\vec{o}$）：**

$$
r_{12} = c_1[c_{23}(-c_4c_5s_6 - s_4c_6) + s_{23}s_5s_6] - s_1[s_4c_5s_6 - c_4c_6]
$$

$$
r_{22} = s_1[c_{23}(-c_4c_5s_6 - s_4c_6) + s_{23}s_5s_6] + c_1[s_4c_5s_6 - c_4c_6]
$$

$$
r_{32} = -s_{23}(-c_4c_5s_6 - s_4c_6) + c_{23}s_5s_6
$$

**第三列（接近向量 $\vec{a}$）：**

$$
r_{13} = c_1[-c_{23}c_4s_5 - s_{23}c_5] - s_1[s_4s_5]
$$

$$
r_{23} = s_1[-c_{23}c_4s_5 - s_{23}c_5] + c_1[s_4s_5]
$$

$$
r_{33} = s_{23}c_4s_5 - c_{23}c_5
$$

---

## 三、逆运动学

### 3.1 求解 $\theta_1$

#### 步骤1：构建方程

从矩阵方程 $T_{10}^{-1}T_{60} = T_{61}$ 的 (2,4) 和 (2,3) 元素：

$$
-s_1p_x + c_1p_y = d_t(s_4s_5) \quad \text{.. .(1)}
$$

$$
-s_1a_x + c_1a_y = s_4s_5 \quad \text{...(2)}
$$

#### 步骤2：消元

将方程(2)代入方程(1)消去 $s_4s_5$：

$$
-s_1p_x + c_1p_y = d_t(-s_1a_x + c_1a_y)
$$

#### 步骤3：整理求解

$$
s_1(d_ta_x - p_x) = c_1(d_ta_y - p_y)
$$

#### 🔹 公式1：$\theta_1$ 求解公式

$$
\boxed{\theta_1 = \text{atan2}(p_y - d_ta_y, \ p_x - d_ta_x)}
$$

> **物理意义**：$(p_x - d_ta_x, p_y - d_ta_y)$ 是腕部中心点在基坐标系XY平面的投影

---

### 3.2 求解 $\theta_3$

#### 步骤1：计算腕部中心坐标

从 $T_{10}^{-1}T_{60} = T_{61}$ 提取位置元素，消去 $\theta_4, \theta_5$ 的影响：

$$
m = (c_1p_x + s_1p_y) - d_t(c_1a_x + s_1a_y) - a_1
$$

$$
n = (p_z - d_1) - d_ta_z
$$

#### 步骤2：建立几何约束

腕部中心仅由 $\theta_2, \theta_3$ 决定：

$$
\begin{cases}
m = a_2c_2 + a_3c_{23} - d_4s_{23} \\
n = -a_2s_2 - a_3s_{23} - d_4c_{23}
\end{cases}
$$

#### 步骤3：利用模长消去 $\theta_2$

计算 $m^2 + n^2$：

$$
m^2 + n^2 = a_2^2 + a_3^2 + d_4^2 + 2a_2[a_3\cos(\theta_3) - d_4\sin(\theta_3)]
$$

#### 步骤4：定义中间变量

$$
K = m^2 + n^2 - a_2^2 - a_3^2 - d_4^2
$$

$$
A = 2a_2a_3, \quad B = -2a_2d_4
$$

方程变为：$A\cos(\theta_3) + B\sin(\theta_3) = K$

#### 🔹 公式2：$\theta_3$ 求解公式

$$
\boxed{\theta_3 = \text{atan2}(-d_4, a_3) \pm \arccos\left(\frac{K}{2a_2\sqrt{a_3^2 + d_4^2}}\right)}
$$

> **注意**：$\pm$ 对应**肘部朝上**和**肘部朝下**两种构型

---

### 3.3 求解 $\theta_2$

#### 步骤1：展开三角函数

利用和角公式展开 $m, n$ 方程，分离 $c_2, s_2$：

$$
m = [a_2 + a_3c_3 - d_4s_3]\cos(\theta_2) + [-a_3s_3 - d_4c_3]\sin(\theta_2)
$$

$$
n = [-a_3s_3 - d_4c_3]\cos(\theta_2) + [-a_2 - a_3c_3 + d_4s_3]\sin(\theta_2)
$$

#### 步骤2：定义系数

$$
k_1 = a_2 + a_3c_3 - d_4s_3
$$

$$
k_2 = -a_3s_3 - d_4c_3
$$

简化为线性方程组：

$$
\begin{cases}
m = k_1c_2 + k_2s_2 \\
n = k_2c_2 - k_1s_2
\end{cases}
$$

#### 步骤3：求解线性方程组

$$
s_2 = \frac{k_2m - k_1n}{k_1^2 + k_2^2}, \quad c_2 = \frac{k_1m + k_2n}{k_1^2 + k_2^2}
$$

#### 🔹 公式3：$\theta_2$ 求解公式

$$
\boxed{\theta_2 = \text{atan2}(k_2m - k_1n, \ k_1m + k_2n)}
$$

**完整变量定义：**
- $m = (c_1p_x + s_1p_y) - d_t(c_1a_x + s_1a_y) - a_1$
- $n = p_z - d_1 - d_ta_z$
- $k_1 = a_2 + a_3\cos(\theta_3) - d_4\sin(\theta_3)$
- $k_2 = -a_3\sin(\theta_3) - d_4\cos(\theta_3)$

---

### 3.4 求解 $\theta_4$

#### 步骤1：构建方程

从矩阵方程 $T_{30}^{-1}T_{60} = T_{63}$ 的第3列元素：

$$
L_{13} = c_{23}(c_1a_x + s_1a_y) - s_{23}a_z = -c_4s_5
$$

$$
L_{33} = -s_1a_x + c_1a_y = s_4s_5
$$

#### 步骤2：利用比值消去 $s_5$

$$
\frac{s_4s_5}{c_4s_5} = \frac{L_{33}}{-L_{13}} = \tan(\theta_4)
$$

#### 🔹 公式4：$\theta_4$ 求解公式

$$
\boxed{\theta_4 = \text{atan2}(L_{33}, -L_{13})}
$$

**展开形式：**

$$
\boxed{\theta_4 = \text{atan2}\Big(-s_1a_x + c_1a_y, \ -[c_{23}(c_1a_x + s_1a_y) - s_{23}a_z]\Big)}
$$

---

### 3.5 求解 $\theta_5$

#### 步骤1：构建方程

从矩阵方程 $T_{40}^{-1}T_{60} = T_{64}$ 的 (1,3) 和 (3,3) 元素：

$$
s_5 = -\text{Row}_1(T_{40}^{-1}) \cdot \vec{a}
$$

$$
c_5 = \text{Row}_3(T_{40}^{-1}) \cdot \vec{a}
$$

#### 步骤2：展开计算

$$
s_5 = -[(c_1c_{23}c_4 + s_1s_4)a_x + (s_1c_{23}c_4 - c_1s_4)a_y + (-s_{23}c_4)a_z]
$$

$$
c_5 = (-c_1s_{23})a_x + (-s_1s_{23})a_y + (-c_{23})a_z
$$

#### 🔹 公式5：$\theta_5$ 求解公式

$$
\boxed{\theta_5 = \text{atan2}(s_5, c_5)}
$$

**其中：**

$$
s_5 = -c_4[c_{23}(c_1a_x + s_1a_y) - s_{23}a_z] - s_4(-s_1a_x + c_1a_y)
$$

$$
c_5 = -s_{23}(c_1a_x + s_1a_y) - c_{23}a_z
$$

---

### 3.6 求解 $\theta_6$

#### 步骤1：构建方程

从矩阵方程 $T_{50}^{-1}T_{60} = T_{65}$ 的第1列元素：

$$
c_6 = u_{11} = \text{Row}_1(T_{50}^{-1}) \cdot \vec{n}
$$

$$
s_6 = -u_{31} = -\text{Row}_3(T_{50}^{-1}) \cdot \vec{n}
$$

#### 步骤2：利用 $T_{50}$ 矩阵

$T_{50}$ 的旋转矩阵元素：

| 元素 | 公式 |
|------|------|
| $r_{11}^{(5)}$ | $c_5(c_1c_{23}c_4 + s_1s_4) - s_5(c_1s_{23})$ |
| $r_{21}^{(5)}$ | $c_5(s_1c_{23}c_4 - c_1s_4) - s_5(s_1s_{23})$ |
| $r_{31}^{(5)}$ | $-c_5s_{23}c_4 + s_5c_{23}$ |
| $r_{13}^{(5)}$ | $c_1c_{23}s_4 - s_1c_4$ |
| $r_{23}^{(5)}$ | $s_1c_{23}s_4 + c_1c_4$ |
| $r_{33}^{(5)}$ | $-s_{23}s_4$ |

#### 步骤3：计算 $u_{11}$ 和 $u_{31}$

$$
u_{11} = r_{11}^{(5)}n_x + r_{21}^{(5)}n_y + r_{31}^{(5)}n_z
$$
$$
u_{31} = r_{13}^{(5)}n_x + r_{23}^{(5)}n_y + r_{33}^{(5)}n_z
$$

$$
u_{11} 
=[c_5(c_1c_{23}c_4 + s_1s_4) - s_5(c_1s_{23})]n_x 
+[c_5(s_1c_{23}c_4 - c_1s_4) - s_5(s_1s_{23})]n_y 
+[-c_5s_{23}c_4 + s_5c_{23}]n_z
$$

$$
u_{31} 
= [c_1c_{23}s_4 - s_1c_4]n_x + [s_1c_{23}s_4 + c_1c_4]n_y + [-s_{23}s_4]n_z
$$



#### 🔹 公式6：$\theta_6$ 求解公式

$$
\boxed{\theta_6 = \text{atan2}(-u_{31}, u_{11})}
$$

---

## 四、公式汇总

### 4.1 逆运动学求解顺序

```
输入：目标位姿 T₆₀ = [n, o, a, p]
          ↓
    ┌─────────────────┐
    │  Step 1: 求 θ₁  │ ← 利用腕部中心XY投影
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │  Step 2: 求 θ₃  │ ← 利用腕部中心模长（有两解）
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │  Step 3: 求 θ₂  │ ← 利用线性方程组
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │  Step 4: 求 θ₄  │ ← 利用接近向量
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │  Step 5: 求 θ₅  │ ← 利用接近向量
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │  Step 6: 求 θ₆  │ ← 利用法向量
    └────────┬────────┘
             ↓
输出：关节角 [θ₁, θ₂, θ₃, θ₄, θ₅, θ₆]
```

### 4.2 核心公式速查表

| 关节 | 公式 | 依赖 |
|------|------|------|
| $\theta_1$ | $\text{atan2}(p_y - d_ta_y, \ p_x - d_ta_x)$ | 仅依赖 $T_{60}$ |
| $\theta_3$ | $\text{atan2}(-d_4, a_3) \pm \arccos\left(\dfrac{K}{2a_2\sqrt{a_3^2 + d_4^2}}\right)$ | 依赖 $\theta_1$ |
| $\theta_2$ | $\text{atan2}(k_2m - k_1n, \ k_1m + k_2n)$ | 依赖 $\theta_1, \theta_3$ |
| $\theta_4$ | $\text{atan2}(L_{33}, -L_{13})$ | 依赖 $\theta_1, \theta_2, \theta_3$ |
| $\theta_5$ | $\text{atan2}(s_5, c_5)$ | 依赖 $\theta_1, \theta_2, \theta_3, \theta_4$ |
| $\theta_6$ | $\text{atan2}(-u_{31}, u_{11})$ | 依赖 $\theta_1 \sim \theta_5$ |

### 4.3 中间变量速查表

| 变量 | 定义 | 用于 |
|------|------|------|
| $m$ | $(c_1p_x + s_1p_y) - d_t(c_1a_x + s_1a_y) - a_1$ | $\theta_2, \theta_3$ |
| $n$ | $p_z - d_1 - d_ta_z$ | $\theta_2, \theta_3$ |
| $K$ | $m^2 + n^2 - a_2^2 - a_3^2 - d_4^2$ | $\theta_3$ |
| $k_1$ | $a_2 + a_3c_3 - d_4s_3$ | $\theta_2$ |
| $k_2$ | $-a_3s_3 - d_4c_3$ | $\theta_2$ |
| $L_{13}$ | $c_{23}(c_1a_x + s_1a_y) - s_{23}a_z$ | $\theta_4, \theta_5$ |
| $L_{33}$ | $-s_1a_x + c_1a_y$ | $\theta_4$ |

---

## 五、多解与奇异性

### 5.1 多解情况

6自由度机械臂通常有**最多8组解**：

| 构型选择 | 解的数量 | 说明 |
|----------|----------|------|
| 肩部构型 | 2 | 左肩/右肩 |
| 肘部构型 | 2 | 肘上/肘下（$\theta_3$ 的 $\pm$）|
| 腕部构型 | 2 | 翻腕/不翻腕 |

### 5.2 奇异位形

| 奇异类型 | 条件 | 影响 |
|----------|------|------|
| 腕部奇异 | $s_5 = 0$（$\theta_5 = 0$ 或 $\pi$）| $\theta_4$ 和 $\theta_6$ 耦合 |
| 肩部奇异 | 腕部中心在Z轴上 | $\theta_1$ 不定 |
| 肘部奇异 | 手臂完全伸直或折叠 | $\theta_3$ 无解或不定 |

### 5.3 奇异处理建议

当 $|s_5| < \epsilon$（$\epsilon$ 为小阈值）时：
- 设 $\theta_4 = \theta_{4,\text{prev}}$（保持上一时刻值）
- 令 $\theta_6 = \text{atan2}(\cdot, \cdot) - \theta_4$

---

## 六、验证方法

### 6.1 正运动学验证

```
给定：θ = [θ₁, θ₂, θ₃, θ₄, θ₅, θ₆]
计算：T₆₀ = T₁₀ · T₂₁ · T₃₂ · T₄₃ · T₅₄ · T₆₅
比较：计算结果与目标位姿是否一致
```

### 6.2 数值验证检查点

1. **位置误差**：$\|p_{\text{calc}} - p_{\text{target}}\| < \epsilon_p$
2. **姿态误差**：$\|R_{\text{calc}} - R_{\text{target}}\|_F < \epsilon_R$
3. **旋转矩阵正交性**：$R^TR = I$，$\det(R) = 1$